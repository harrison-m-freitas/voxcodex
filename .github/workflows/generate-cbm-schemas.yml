name: Superseded CBM Schema Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded branch guard
        run: |
          echo "This branch is superseded and retained only for historical reference."
          echo "Normative lineage: m2-plan04-cbm-materialization -> m2-plan05-reprocessing-provenance -> m2-plan06-corpus-candidate-freeze"
          echo "Original final SHA archived at: archive/m2-plan04-materialization-validation-70b75d4"
