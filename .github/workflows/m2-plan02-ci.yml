name: Superseded M2 CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Superseded branch guard
        run: |
          echo "Historical branch only. Do not use as an implementation base."
          echo "Normative lineage continues from m2-plan04-cbm-materialization."
