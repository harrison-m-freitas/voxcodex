# SUPERSEDED — historical Plan 04 line

This branch is retained only as a historical record of an earlier alternative implementation of CBM v0.1.

Do **not** merge this branch into the current M2 implementation line.

Normative implementation lineage:

`m2-plan04-cbm-materialization` → `m2-plan05-reprocessing-provenance` → `m2-plan06-corpus-candidate-freeze`

The original pre-tombstone branch state is preserved exactly at:

- branch: `archive/m2-plan04-materialization-validation-70b75d4`
- commit: `70b75d4ef8de91d7fb74df817c70e50d91befa88`

Reason for supersession: this branch contains an earlier alternative CBM v0.1 schema/materialization approach. The consolidated Plan 04 implementation has its own canonical gate evidence and is the only supported basis for subsequent M2 plans.

The previous schema-generation workflow on this branch has been disabled from automatic push execution to prevent accidental self-mutation.
